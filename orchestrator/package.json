{
  "name": "juno-orchestrator",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.13"
  },
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "spawn": "node dist/spawn-run.js",
    "preinstall": "node ../scripts/check-package-manager.mjs && node ../scripts/check-orchestrator-deps.mjs"
  },
  "dependencies": {
    "@openai/codex-sdk": "^0.144.1",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
