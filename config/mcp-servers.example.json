{
  "defaultForAgents": ["github"],
  "servers": [
    {
      "id": "github",
      "server": "github",
      "enabled": true,
      "devOnly": true,
      "missions": ["juno-overseer-hardening-2026", "juno-self-iterate-p2-2026"],
      "notes": "Use only when the current Codex session exposes the GitHub MCP server"
    }
  ],
  "notes": "Copy to AgentWorkbench/config/mcp-servers.json. Entries are capability hints, not proof that a server is mounted."
}
